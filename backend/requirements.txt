fastapi==0.111.0
uvicorn[standard]==0.30.1
SQLAlchemy==2.0.30
pydantic[email]==2.7.1
passlib[bcrypt]==1.7.4
# Pin bcrypt: passlib 1.7.4 is incompatible with bcrypt>=4.1 (its self-test
# raises "password cannot be longer than 72 bytes"). 4.0.1 works cleanly.
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
# TOTP second factor for administrator accounts (HIPAA §164.312(d)).
pyotp==2.9.0
python-multipart==0.0.9
python-dotenv==1.0.1
psycopg2-binary==2.9.9
PyMySQL==1.1.1
cryptography==42.0.8
# OMR sheet extraction: PDF page rendering + image handling + AI HTTP calls.
pypdfium2==4.30.0
pillow==10.4.0
httpx==0.27.0
